server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3308/accounts_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: nexabank
    password: nexabank123
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
  http:
    serviceclient:
      customer: # ← must match group = "customer" in @ImportHttpServices
        base-url: http://customer-service