server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3307/customers_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: nexabank
    password: nexabank123
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs